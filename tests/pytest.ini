[pytest]
markers =
    live: marks tests as requiring live API access (may be slow, requires network)
    slow: marks tests as slow-running

# Default: run all tests
# To skip live tests: pytest -m "not live"
# To run only live tests: pytest -m "live"

# Show extra test summary for failures
addopts = --tb=short
